import React from 'react';
import ProfileComponent from 'features/employer/profile';

const Profile = () => {
  return (
    <React.Fragment>
      <ProfileComponent />
    </React.Fragment>
  );
};

export default Profile;
